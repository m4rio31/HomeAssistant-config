language: python

python: "3.7"

before_install:
- mv travis_secrets.yaml secrets.yaml
- sudo apt-get install -y libudev-dev

install:
- pip3 install homeassistant==2021.12.1

script:
- hass -c . --script check_config